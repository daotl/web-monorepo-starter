{
  "name": "web",
  "sourceRoot": "apps/web/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "./apps/web",
        "command": "mkdir dist && echo 'app' > dist/index.js"
      }
    },
    "lint": {
      "command": "echo lint",
      "options": {}
    }
  }
}
