{
  "name": "desktop",
  "sourceRoot": "apps/desktop/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "./apps/desktop",
        "command": "mkdir dist && echo 'app' > dist/index.js"
      }
    },
    "lint": {
      "command": "echo lint",
      "options": {}
    }
  }
}
